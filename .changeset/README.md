# Changesets

Add a Changeset for user-visible behavior, capability-contract, event-schema, or CLI changes:

```bash
pnpm changeset
```

The release workflow turns merged Changesets into a version pull request. See [the versioning policy](../docs/versioning.md).
